task runGradleTest

node {
	useLatestNode = "true"
}

runGradleTest {
	dependsOn downloadNode

	doLast {
		assert node.nodeVersion != null
		assert node.nodeVersion != "16.13.0"
		assert node.nodeVersion.startsWith("2")
	}

}